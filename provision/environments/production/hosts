all:
  hosts:
    default:
      ansible_host: mcbalcaldi.kerri.uy
      ansible_user: mcbalcaldi
  children:
    dbservers:
      hosts:
        default
    webservers:
      hosts:
        default:
